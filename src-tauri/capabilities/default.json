{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "None. The window is a browser onto the local server and calls no Tauri API; the server is started from Rust, which capabilities do not gate.",
  "windows": ["main"],
  "permissions": []
}
