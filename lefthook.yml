# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      run: bun oxlint --fix {staged_files}
      glob: "*.{ts,tsx,js,jsx}"
      stage_fixed: true
    - name: oxfmt
      run: 'if [ -n "{staged_files}" ]; then bun oxfmt --write {staged_files}; fi'
      glob: "*.{ts,tsx,js,jsx,json}"
      stage_fixed: true
